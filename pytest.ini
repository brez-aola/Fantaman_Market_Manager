[pytest]
# Avoid loading the global 'deepeval' plugin which is installed system-wide
addopts = -p no:deepeval

# Markers used in this repository's test suite
markers =
    cli: marks tests that exercise CLI behavior (run separately or in CI)

# Silence some noise during local test runs
filterwarnings =
    ignore::DeprecationWarning
